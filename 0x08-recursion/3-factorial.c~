#include "main.h"

/**
 * factorial - returns factorial of a given number
 * @n: integer n
 * Return: integer number of n
 */

int factorial(int n)
{
	if (n == 0)
		return (0);
	else if (n > 0)
		return (n * factorial(n - 1));
	else
		return (-1);
}
